name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: pgvector/pgvector:pg17
        env:
          POSTGRES_USER: personus
          POSTGRES_PASSWORD: personus
          POSTGRES_DB: personus_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U personus"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    env:
      # Enables the service-layer integration suite (harness skips without it).
      TEST_DATABASE_URL: postgresql://personus:personus@localhost:5432/personus_test

    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14
      - name: Install
        run: bun install --frozen-lockfile
      - name: Type-check
        run: bun run type-check
      - name: Lint
        run: bunx @biomejs/biome check .
      - name: Validate docs bundle (OKF)
        run: bun run docs:validate
      - name: Authz surface guard (every route/action resolves a principal)
        run: bun run authz:check
      - name: Test (unit + integration)
        run: bun run test
      - name: Coverage
        run: bun run test:coverage

  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14
      - name: Install
        run: bun install --frozen-lockfile
      - name: Install Playwright browser
        run: bunx playwright install --with-deps chromium
        working-directory: apps/web
      - name: E2E (credential-free)
        run: bun run test:e2e
        working-directory: apps/web
