---
type: prd
title: Core PRD (source document — needs conversion)
description: Pointer to the original binary Core PRD, which could not be ported as Markdown.
status: superseded
tags: [business-model]
timestamp: 2026-07-24
---

# Core PRD — source document (needs conversion)

The original Personus Core PRD exists only as a binary Word document and could **not** be ported into this Markdown bundle:

- **Source:** `current/personus/docs/Personus_ai_Core_PRD_v1_0.docx` (legacy repo)

Its content predates the decomposed spec suite and is largely superseded by the current [foundation](/foundation/vision.md) docs, the [business-model](/business-model/01_executive_summary.md) set, and the per-area PRDs under [`domains/`](/domains/_areas.md). It is recorded here so the provenance is not lost.

**To bring it in:** open the `.docx`, export to Markdown, reconcile against the current specs, and replace this pointer with the resulting doc (or fold anything unique into the existing PRDs). Track as a follow-up if the material turns out to be needed.
